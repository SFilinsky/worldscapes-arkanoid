{
  "ignore": ["**/*.test.ts", "**/*.spec.ts"],
  "watch": ["./src", "./node_modules", "../common/dist"],
  "exec": "npm run build && npx kill-port 50002 && npm run launch",
  "ext": "ts",
  "delay": 500
}